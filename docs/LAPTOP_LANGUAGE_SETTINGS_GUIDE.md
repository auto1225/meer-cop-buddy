# 🌐 노트북 앱 언어 설정 연동 가이드

## 📋 개요

스마트폰 앱에서 설정한 언어가 노트북 앱에 실시간으로 반영되는 구조를 설명합니다.
현재 노트북 앱은 **한국어(ko)** / **영어(en)** 2개 언어를 정적으로 지원하며,
스마트폰에서 17개 언어 중 하나를 선택하면 노트북이 이를 수신하여 UI를 갱신합니다.

---

## 1️⃣ 지원 언어 목록

| 코드 | 언어 | 네이티브 표기 | RTL |
|------|------|-------------|-----|
| `ko` | 한국어 | 한국어 | ❌ |
| `en` | English | English | ❌ |
| `ja` | 日本語 | 日本語 | ❌ |
| `zh` | 中文 | 中文 | ❌ |
| `es` | Español | Español | ❌ |
| `fr` | Français | Français | ❌ |
| `de` | Deutsch | Deutsch | ❌ |
| `pt` | Português | Português | ❌ |
| `ru` | Русский | Русский | ❌ |
| `vi` | Tiếng Việt | Tiếng Việt | ❌ |
| `th` | ไทย | ไทย | ❌ |
| `id` | Bahasa Indonesia | Bahasa Indonesia | ❌ |
| `ms` | Bahasa Melayu | Bahasa Melayu | ❌ |
| `hi` | हिन्दी | हिन्दी | ❌ |
| `tr` | Türkçe | Türkçe | ❌ |
| `ar` | العربية | العربية | ✅ |
| `it` | Italiano | Italiano | ❌ |

---

## 2️⃣ 수신 방법

### 2-1. 실시간 Broadcast (즉시 반영)

스마트폰이 언어를 변경하면 `device-commands-${deviceId}` 채널로 브로드캐스트를 전송합니다.

```
채널: device-commands-${deviceId}
이벤트: settings_updated
페이로드: { type: "settings_updated" }
```

노트북은 이 이벤트 수신 시 `refetch()`를 호출하여 DB에서 최신 metadata를 읽고,
`metadata.language` 값을 추출하여 앱 언어를 갱신합니다.

### 2-2. DB 초기 로드 (앱 시작 시)

```sql
SELECT metadata FROM devices WHERE device_id = '${deviceId}'
-- metadata.language → "ko" | "en" | "ja" | ... 
```

앱 시작 시 DB에서 `metadata.language`를 읽어 초기 언어를 설정합니다.
값이 없으면 `localStorage('meercop-language')` → 기본값 `"ko"` 순서로 폴백합니다.

### 2-3. localStorage 동기화

```
키: meercop-language
값: "ko" | "en" | ...
```

언어 변경 시 항상 localStorage에도 저장하여 오프라인 상태에서도 마지막 설정을 유지합니다.

---

## 3️⃣ 번역 시스템 (정적 번들 방식)

모든 17개 언어를 **빌드 타임에 포함된 정적 JSON 파일**로 관리합니다.
AI 동적 번역(Edge Function)은 사용하지 않습니다.

### 3-1. 파일 구조

```
src/
  locales/
    ko.json
    en.json
    ja.json
    zh.json
    es.json
    fr.json
    de.json
    pt.json
    ru.json
    vi.json
    th.json
    id.json
    ms.json
    hi.json
    tr.json
    ar.json
    it.json
```

### 3-2. JSON 파일 형식

```json
// src/locales/ko.json
{
  "settings.title": "설정",
  "settings.device_type": "기기 타입",
  "settings.alarm_sound": "경보음",
  "settings.sensors": "감지 센서",
  "monitoring.start": "감시 시작",
  "monitoring.stop": "감시 중지",
  "alert.dismiss": "경보 해제"
}
```

### 3-3. 로딩 방식

```typescript
// 동적 import로 필요한 언어만 로드
const loadTranslations = async (lang: string) => {
  const module = await import(`@/locales/${lang}.json`);
  return module.default;
};
```

- 빌드 시 Vite가 각 JSON을 청크로 분리하여 번들에 포함
- 런타임에 `import()`로 해당 언어 청크만 로드 (네트워크 요청 없음)
- 별도의 캐시/TTL 관리 불필요

---

## 4️⃣ 노트북 전용 번역 키

스마트폰과 공유하지 않는 노트북 전용 UI 키입니다.
(`docs/I18N_SHARED_KEYS_GUIDE.md`의 공유 키와 별도로 관리)

### 경보 화면

| 키 | ko | en |
|---|---|---|
| `alert.screen.title` | ⚠️ 경보 발생! | ⚠️ Alert! |
| `alert.screen.enter_pin` | PIN을 입력하세요 | Enter PIN |
| `alert.screen.wrong_pin` | PIN이 틀렸습니다 | Wrong PIN |
| `alert.screen.locked` | 잠금 상태입니다 | Device Locked |

### 잠금 화면

| 키 | ko | en |
|---|---|---|
| `lock.remote_locked` | 원격 잠금됨 | Remotely Locked |
| `lock.enter_pin_to_unlock` | 해제하려면 PIN 입력 | Enter PIN to unlock |

### 메시지 표시

| 키 | ko | en |
|---|---|---|
| `message.from_phone` | 📱 스마트폰 메시지 | 📱 Message from Phone |

### 시리얼 인증 화면

| 키 | ko | en |
|---|---|---|
| `auth.title` | MeerCOP 인증 | MeerCOP Authentication |
| `auth.enter_serial` | 시리얼 번호를 입력하세요 | Enter Serial Number |
| `auth.connect` | 연결 | Connect |

### 설정 패널

| 키 | ko | en |
|---|---|---|
| `settings.language` | 언어 / Language | Language |
| `settings.volume` | 볼륨 | Volume |
| `settings.custom_sound_upload` | 내 기기에서 경보음 선택... | Choose alarm sound from device... |
| `settings.change_from_phone` | 스마트폰에서 변경 | Change from smartphone |
| `settings.motion_test` | 🔬 모션 테스트 | 🔬 Motion Test |

---

## 5️⃣ UI/UX 디자인 — 화면별 다국어 레이아웃

### 5-1. 설정 패널 (SensorSettingsPanel)

```
┌─────────────────────────────┐
│  설정 / Settings          ✕ │
├─────────────────────────────┤
│  기기 타입 / Device Type    │
│  [노트북] [데스크탑] [태블릿]│
├─────────────────────────────┤
│  경보음 / Alarm Sound       │
│  🔊 ━━━━━━━━━━●━━━ 75%     │
│  ┌───────────────────────┐  │
│  │ 경찰 사이렌      ▶️   │  │
│  │ 비상 경보        ▶️   │  │
│  │ 🎵 내 경보음     ▶️ 🗑│  │
│  │ [📁 파일에서 선택...]  │  │
│  └───────────────────────┘  │
├─────────────────────────────┤
│  감지 센서 / Sensors        │
│  📷 카메라 모션    [━━━●]   │
│  💻 덮개 감지      [━━━●]   │
│  🎤 마이크         [━●━━]   │
├─────────────────────────────┤
│  🌐 언어 / Language         │
│  [한국어] [English]         │
│  ℹ️ 스마트폰에서도 변경 가능 │
└─────────────────────────────┘
```

### 5-2. 경보 화면 (AlertScreen)

```
┌─────────────────────────────┐
│                             │
│       ⚠️ 경보 발생!         │
│       ⚠️ Alert!             │
│                             │
│    카메라 모션이 감지됨      │
│    Camera motion detected   │
│                             │
│    ┌─┐ ┌─┐ ┌─┐ ┌─┐        │
│    │ │ │ │ │ │ │ │  PIN    │
│    └─┘ └─┘ └─┘ └─┘        │
│    [1][2][3]               │
│    [4][5][6]               │
│    [7][8][9]               │
│    [←][0][✓]               │
│                             │
└─────────────────────────────┘
```

### 5-3. 메인 감시 화면 (Index)

```
┌─────────────────────────────┐
│ MeerCOP  [📷][🔊][📶] [⚙️] │
├─────────────────────────────┤
│                             │
│     🦝 MeerCOP 마스코트      │
│                             │
│   "감시 중... / Monitoring" │
│                             │
│   ┌─────────────────────┐   │
│   │  [감시 시작/중지]    │   │
│   │  Start / Stop       │   │
│   └─────────────────────┘   │
│                             │
└─────────────────────────────┘
```

---

## 6️⃣ RTL (Right-to-Left) 대응

아랍어(`ar`) 선택 시 레이아웃을 반전합니다.

```typescript
// HTML dir 속성 설정
useEffect(() => {
  const rtlLanguages = ["ar"];
  document.documentElement.dir = rtlLanguages.includes(appLanguage) ? "rtl" : "ltr";
}, [appLanguage]);
```

### RTL 레이아웃 예시

```
┌─────────────────────────────┐
│ ✕          الإعدادات        │  ← 제목 오른쪽 정렬
├─────────────────────────────┤
│         نوع الجهاز          │
│ [تابلت] [مكتبي] [لابتوب]   │  ← 버튼 순서 반전
├─────────────────────────────┤
│           أجهزة الاستشعار   │
│  [●━━━]  حركة الكاميرا 📷  │  ← 아이콘-텍스트 순서 반전
│  [●━━━]    غطاء الجهاز 💻  │
└─────────────────────────────┘
```

### Tailwind RTL 유틸리티

```css
/* 방향 인식 마진/패딩 */
.ms-2  → margin-inline-start: 0.5rem  (LTR: left, RTL: right)
.me-2  → margin-inline-end: 0.5rem    (LTR: right, RTL: left)
.ps-3  → padding-inline-start: 0.75rem
.pe-3  → padding-inline-end: 0.75rem
```

---

## 7️⃣ 폰트 가이드

| 언어군 | 권장 폰트 | 폴백 |
|--------|----------|------|
| 한국어 | Pretendard, Noto Sans KR | system-ui |
| 일본어 | Noto Sans JP | system-ui |
| 중국어 | Noto Sans SC / TC | system-ui |
| 아랍어 | Noto Sans Arabic | system-ui |
| 힌디어 | Noto Sans Devanagari | system-ui |
| 태국어 | Noto Sans Thai | system-ui |
| 기타 라틴 | Inter, system-ui | sans-serif |

```css
/* 동적 폰트 로드 예시 */
@font-face {
  font-family: 'Noto Sans JP';
  src: url('https://fonts.googleapis.com/css2?family=Noto+Sans+JP&display=swap');
  font-display: swap;
}
```

---

## 8️⃣ 구현 체크리스트

- [ ] 1. `metadata.language` 값을 DB에서 읽어 초기 언어 설정
- [ ] 2. `settings_updated` broadcast 수신 시 언어 갱신
- [ ] 3. localStorage(`meercop-language`) 동기화
- [ ] 4. 17개 언어 정적 JSON 번역 파일 생성 (`src/locales/*.json`)
- [ ] 5. 동적 `import()`를 통한 언어 파일 로딩
- [ ] 6. 설정 패널 언어 선택 UI
- [ ] 7. 경보 화면 다국어 표시
- [ ] 8. 잠금 화면 다국어 표시
- [ ] 9. 시리얼 인증 화면 다국어 표시
- [ ] 10. RTL 대응 (아랍어)
- [ ] 11. 동적 폰트 로딩

---

## ⚠️ 주의사항

1. **성능**: 정적 JSON은 Vite 빌드에 포함되어 네트워크 요청 없이 즉시 로드됩니다
2. **오프라인**: 번들에 포함되므로 오프라인에서도 모든 언어가 정상 동작합니다
3. **일관성**: `docs/I18N_SHARED_KEYS_GUIDE.md`의 공유 키와 충돌하지 않도록 주의합니다
4. **테스트**: 긴 번역 텍스트가 UI를 깨뜨리지 않는지 확인합니다 (독일어, 아랍어 등)
5. **번역 추가**: 새 키 추가 시 17개 JSON 파일 모두에 해당 키를 추가해야 합니다
